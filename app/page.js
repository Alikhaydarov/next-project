'use client';import {useEffect,useMemo,useState} from 'react';import * as XLSX from 'xlsx';
const KEY='payflow-v3',blank={mode:'local',companies:[],our_accounts:[],company_accounts:[],payments:[]};
const won=n=>'₩'+Number(n||0).toLocaleString('en-US');const now=()=>{let d=new Date;d.setMinutes(d.getMinutes()-d.getTimezoneOffset());return d.toISOString().slice(0,16)};
export default function Home(){const [d,setD]=useState(blank),[tab,setTab]=useState('dashboard'),[company,setCompany]=useState(''),[ours,setOurs]=useState(''),[theirs,setTheirs]=useState(''),[date,setDate]=useState(now()),[amount,setAmount]=useState(''),[q,setQ]=useState(''),[msg,setMsg]=useState('');
const saveLocal=n=>{setD(n);localStorage.setItem(KEY,JSON.stringify(n))};async function load(){try{let r=await fetch('/api/data',{cache:'no-store'}),j=await r.json();if(r.ok&&j.mode==='database'){setD(j);return j}}catch{}let j=JSON.parse(localStorage.getItem(KEY)||'null')||blank;setD(j);return j}useEffect(()=>{load()},[]);useEffect(()=>setTheirs(''),[company]);
const ca=d.company_accounts.filter(x=>String(x.company_id)===company),stats=useMemo(()=>{let t=new Date(),day=d.payments.filter(x=>new Date(x.paid_at).toDateString()===t.toDateString()).reduce((s,x)=>s+x.amount,0),month=d.payments.filter(x=>{let z=new Date(x.paid_at);return z.getMonth()===t.getMonth()&&z.getFullYear()===t.getFullYear()}).reduce((s,x)=>s+x.amount,0),total=d.payments.reduce((s,x)=>s+x.amount,0);return{day,month,total,count:d.payments.length}},[d.payments]),rows=d.payments.filter(x=>!q||Object.values(x).join(' ').toLowerCase().includes(q.toLowerCase()));
const flash=m=>{setMsg(m);setTimeout(()=>setMsg(''),2200)};async function api(body){let r=await fetch('/api/mutate',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)}),j=await r.json();if(!r.ok)throw Error(j.error||'Failed')}
function excel(payments=d.payments){let out=payments.map((p,i)=>({'ID':i+1,'Company Name':p.company_name,'Our Card Account':p.our_account,'Company Card Account':p.company_account,'Date & Time':new Date(p.paid_at).toLocaleString('en-GB'),'Amount':p.amount})),wb=XLSX.utils.book_new(),ws=XLSX.utils.json_to_sheet(out);ws['!cols']=[8,24,24,26,22,16].map(wch=>({wch}));XLSX.utils.book_append_sheet(wb,ws,'Payments');XLSX.writeFile(wb,'payments.xlsx')}
async function add(type,value,cid){if(!value.trim())return;try{if(d.mode==='database'){await api(type==='company'?{action:'company',name:value}:type==='our'?{action:'our',label:value}:{action:'their',company_id:cid,label:value});await load()}else{let id=Date.now(),n={...d};if(type==='company')n.companies=[...d.companies,{id,name:value}];if(type==='our')n.our_accounts=[...d.our_accounts,{id,label:value}];if(type==='their')n.company_accounts=[...d.company_accounts,{id,company_id:Number(cid),label:value}];saveLocal(n)}flash('Saved')}catch(e){alert(e.message)}}
async function submit(e){e.preventDefault();let a=Number(amount.replace(/\D/g,''));if(!company||!ours||!theirs||!date||!a)return flash('Complete all fields');if(d.mode==='database'){await api({action:'payment',company_id:company,our_account_id:ours,company_account_id:theirs,paid_at:date,amount:a});let fresh=await load();setAmount('');setDate(now());flash('Payment saved');setTimeout(()=>excel(fresh.payments),80)}else{let c=d.companies.find(x=>String(x.id)===company),o=d.our_accounts.find(x=>String(x.id)===ours),t=d.company_accounts.find(x=>String(x.id)===theirs),n={...d,payments:[{id:Date.now(),company_name:c.name,our_account:o.label,company_account:t.label,paid_at:new Date(date).toISOString(),amount:a},...d.payments]};saveLocal(n);setAmount('');setDate(now());flash('Payment saved');setTimeout(()=>excel(n.payments),80)}}
return <div className="shell"><aside><div className="brand"><b>P</b><div><strong>PayFlow</strong><small>PAYMENT MANAGEMENT</small></div></div><nav><button className={tab==='dashboard'?'on':''} onClick={()=>setTab('dashboard')}>▦ Dashboard</button><button className={tab==='settings'?'on':''} onClick={()=>setTab('settings')}>⚙ Settings</button></nav><div className="mode"><i className={d.mode}/><b>{d.mode==='database'?'Database mode':'Local mode'}</b><p>{d.mode==='database'?'Stored online':'Stored in this browser'}</p></div></aside><main>{tab==='dashboard'?<><header><div><em>FINANCE OPERATIONS</em><h1>Payment Dashboard</h1><p>Record company transfers and keep the full history export-ready.</p></div><div><button onClick={()=>excel()}>↓ Download Excel</button><button className="primary" onClick={()=>document.querySelector('.formcard').scrollIntoView({behavior:'smooth'})}>+ New Payment</button></div></header><section className="stats">{[['Today',won(stats.day)],['This Month',won(stats.month)],['All-Time Total',won(stats.total)],['Records',stats.count]].map(([a,b])=><article key={a}><span>{a}</span><strong>{b}</strong></article>)}</section><section className="grid"><div className="card formcard"><div className="cardhead"><div><small>NEW TRANSACTION</small><h2>Record Payment</h2></div><b>Excel auto-export</b></div><form onSubmit={submit}>{[['1','Company Name',<select value={company} onChange={e=>setCompany(e.target.value)}><option value="">Select company</option>{d.companies.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select>],['2','Our Card Account',<select value={ours} onChange={e=>setOurs(e.target.value)}><option value="">Select our account</option>{d.our_accounts.map(x=><option key={x.id} value={x.id}>{x.label}</option>)}</select>],['3','Company Card Account',<select value={theirs} onChange={e=>setTheirs(e.target.value)} disabled={!company}><option value="">Select company account</option>{ca.map(x=><option key={x.id} value={x.id}>{x.label}</option>)}</select>],['4','Date & Time',<input type="datetime-local" value={date} onChange={e=>setDate(e.target.value)}/>],['5','Amount',<div className="money"><span>₩</span><input value={amount} placeholder="1,500,000" onChange={e=>{let v=e.target.value.replace(/\D/g,'');setAmount(v?Number(v).toLocaleString('en-US'):'')}}/></div>]].map(([n,l,c])=><label key={n}><span><i>{n}</i>{l}</span>{c}</label>)}<button className="save">Save Payment & Download Excel</button></form></div><div className="card history"><div className="cardhead"><div><small>TRANSACTIONS</small><h2>Payment History</h2></div><input placeholder="Search..." value={q} onChange={e=>setQ(e.target.value)}/></div><div className="table"><table><thead><tr><th>Company</th><th>Our Account</th><th>Company Account</th><th>Date & Time</th><th>Amount</th></tr></thead><tbody>{rows.length?rows.map(x=><tr key={x.id}><td><b>{x.company_name}</b></td><td>{x.our_account}</td><td>{x.company_account}</td><td>{new Date(x.paid_at).toLocaleString('en-GB')}</td><td>{won(x.amount)}</td></tr>):<tr><td colSpan="5" className="empty">No payments yet.</td></tr>}</tbody></table></div></div></section></>:<Settings d={d} add={add}/>}</main>{msg&&<div className="toast">✓ {msg}</div>}</div>}
function Settings({d,add}){let [c,setC]=useState(''),[o,setO]=useState(''),[cid,setCid]=useState(''),[t,setT]=useState('');return <><header><div><em>CONFIGURATION</em><h1>Settings</h1><p>Manage companies and card accounts.</p></div></header><section className="settings"><Box title="Companies" count={d.companies.length}><form onSubmit={e=>{e.preventDefault();add('company',c);setC('')}}><input placeholder="Company name" value={c} onChange={e=>setC(e.target.value)}/><button>Add</button></form>{d.companies.map(x=><p key={x.id}><b>{x.name}</b><span>#{x.id}</span></p>)}</Box><Box title="Our Card Accounts" count={d.our_accounts.length}><form onSubmit={e=>{e.preventDefault();add('our',o);setO('')}}><input placeholder="Shinhan •••• 1234" value={o} onChange={e=>setO(e.target.value)}/><button>Add</button></form>{d.our_accounts.map(x=><p key={x.id}><b>{x.label}</b><span>#{x.id}</span></p>)}</Box><Box title="Company Card Accounts" count={d.company_accounts.length}><select value={cid} onChange={e=>setCid(e.target.value)}><option value="">Select company</option>{d.companies.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><form onSubmit={e=>{e.preventDefault();add('their',t,cid);setT('')}}><input placeholder="KB •••• 8821" value={t} onChange={e=>setT(e.target.value)}/><button>Add</button></form>{d.company_accounts.map(x=><p key={x.id}><b>{x.label}</b><span>{d.companies.find(c=>c.id===x.company_id)?.name}</span></p>)}</Box></section></>}
function Box({title,count,children}){return <div className="card box"><div className="cardhead"><h2>{title}</h2><b>{count}</b></div><div className="boxbody">{children}</div></div>}
